<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="qna">

	<!-- 시퀀스 등록 -->
	<select id="sequence" resultType="int">
		select qna_seq.nextval from dual
	</select>
	
	
	<!-- 등록 -->
	<select id="write" resultType="QnaDto">
		insert into qna(
			qna_no, qna_writer, qna_category, qna_title, qna_answer
		)
		values (
			#{qnaNo}, #{qnaWriter}, #{qnaCategory}, #{qnaTitle}, #{qnaAnswer}
		)
	</select>


	<!-- 조회 -->	
	<select id="list" resultType="QnaDto">
		select * from qna order by qna_no asc
	</select>

	
	<!-- 상세 -->
	<select id="detail" resultType="QnaDto">
		select * from qna where qna_no = #{qnaNo}
	</select>
	
	
	<!-- 삭제 -->
	<delete id="delete">
		delete qna where qna_no = #{qnaNo}
	</delete>
	
	
	<!-- 수정 -->
	<update id="change">
		update qna
		<set>
			<if test="qnaDto.qnaCategory != null">
				qna_category = #{qnaDto.qnaCategory},
			</if>
			<if test="qnaDto.qnaTitle != null">
				qna_title = #{qnaDto.qnaTitle},
			</if>
			<if test="qnaDto.qnaAnswer != null">
				qna_answer = #{qnaDto.qnaAnser},
			</if>
		</set>
		where qna_no = #{qnaNo}
	</update>
	
	
	<!-- 검색 -->
	<select id="search" resultType="QnaDto">
		select * from qna where instr(qna_title, #{qnaTitle}) > 0
	</select>
	
	
	<!-- 페이징 -->
	<select id="page" resultType="QnaDto">
		select * from (
			select rownum rn, TMP.* from (
				select * from qna order by qna_no asc
			)TMP
		) where rn between #{begin} and #{end}
	</select>
	
	
</mapper>