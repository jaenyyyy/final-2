<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace ="workday">

   <select id="sequence" resultType="int">
    	select workday_seq.nextval from dual
    </select>
    
<!-- 영업요일 추가 -->
	<insert id="addWorkday">
	insert into workday(workday_no, workday_name, not_workday, work_res_no) 
	values(workday_seq.nextval, #{workdayName}, #{notWorkday}, #{workResNo})	
	</insert>
	
	<delete id ="deleteWorkday">
	delete from workday where where workday_no=#{workdayNo}
	</delete>
	
	<select id="findWorkday" resultType="WorkdayDto">
	select from workday where workday_no=#{workdayNo}
		</select>
	
	<select id="findAllWorkday" resultType ="WorkdayDto">
	select * from workday where work_res_no=#{workResNo}
	</select>
	
	<update id ="editWorkday">
	UPDATE workday SET
		workday_no = #{workdayNo},
		workday_name = #{workdayName},
		not_workday = #{notWorkday} 
		 where work_res_no = #{workResNo}
	</update>
	</mapper>